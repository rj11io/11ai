# runner.conf — provider config for run-tick.sh (git-ignore this file).
# The conductor never sees these; only the runner does. Pick ONE agent line.

AGENT_CMD='claude -p'
# AGENT_CMD='codex exec --sandbox workspace-write'
# AGENT_CMD='gemini --approval-mode auto_edit -p'
# AGENT_CMD='opencode run'

# Optional: a different provider for the review step — a second model family
# rubber-stamps less than the author reviewing itself.
REVIEWER_CMD='codex exec --sandbox read-only'

# Map the ledger's modelTier to real model ids here, and only here.
# MODEL_TIER=capable

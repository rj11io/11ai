interface:
  display_name: "11ai Compression"
  short_description: "Compress markdown guidance without external CLI"
  default_prompt: "Use $11ai-compression to compress a markdown guidance file locally while preserving headings, code, links, and paths."

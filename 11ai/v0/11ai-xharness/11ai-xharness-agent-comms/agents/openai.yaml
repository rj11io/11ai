interface:
  display_name: "11ai Cross-Harness Agent Comms"
  short_description: "Call an agent in another harness and collect its output"
  default_prompt: "Use $11ai-xharness-agent-comms to delegate this task to an agent in another harness and return its output."

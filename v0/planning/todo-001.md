The template openai.yaml under 11ai-cleanup-creator/assets/ — it's an intentional scaffolding asset, correctly outside the validated path.
Unused validator support for icons/brand color/implicit-invocation policy — harmless capacity for the future.
The bigger headroom items (Cowork .plugin packaging, Codex policy.products, screenshots/logos, privacy/terms URLs, Claude marketplace tags/displayName) — those are feature adoption decisions, not defects; say the word if you want any of them.
